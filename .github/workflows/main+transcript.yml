name: Record and Transcribe VOT

on:
  workflow_dispatch:
  schedule:
    - cron: "0 5 * * *"   # Tiap hari jam 05:00 UTC (~13:00 WITA)

jobs:
  record:
    name: 🎙️ Record Stream and Upload
    runs-on: ubuntu-latest
    strategy:
      matrix:
        suffix: ["-s 4 -p 4", "-s 5 -p 5", "-s 6 -p 6"]
    outputs:
      url_s4p4: ${{ steps.extract.outputs.url_s4p4 }}
      # url_s5p5: ${{ steps.extract.outputs.url_s5p5 }}
      # url_s6p6: ${{ steps.extract.outputs.url_s6p6 }}

    steps:
      - name: Download Python recorder & FFmpeg
        run: |
          curl -L -o record.py https://raw.githubusercontent.com/Sparkplugx1904/voiceoftrisma/main/main/record.py
          curl -L -o ffmpeg https://github.com/Sparkplugx1904/voiceoftrisma/raw/refs/heads/main/bin/ffmpeg
          curl -L -o ffprobe https://github.com/Sparkplugx1904/voiceoftrisma/raw/refs/heads/main/bin/ffprobe
          curl -L -o requirements.txt https://raw.githubusercontent.com/Sparkplugx1904/voiceoftrisma/main/requirements.txt
          chmod +x ffmpeg ffprobe

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run recorder
        id: record
        env:
          MY_ACCESS_KEY: ${{ secrets.MY_ACCESS_KEY }}
          MY_SECRET_KEY: ${{ secrets.MY_SECRET_KEY }}
        run: |
          mkdir -p recordings
          python3 record.py ${{ matrix.suffix }}

      - name: Extract uploaded URL
        id: extract
        run: |
          # Ambil URL Archive.org dari log Python
          url=$(grep -o 'https://archive.org/details/[^ ]*' record.py.log | tail -n 1 || echo "https://archive.org/details/UNKNOWN")

          # Ubah "details" menjadi "download" agar URL langsung menunjuk ke file audio
          url="${url/details/download}"

          echo "[+] Archive URL ditemukan (download): ${url}"

          # Mapkan URL sesuai suffix agar bisa diakses lintas job
          if [[ "${{ matrix.suffix }}" == "-s 4 -p 4" ]]; then
            echo "url_s4p4=${url}" >> $GITHUB_OUTPUT
          elif [[ "${{ matrix.suffix }}" == "-s 5 -p 5" ]]; then
            echo "url_s5p5=${url}" >> $GITHUB_OUTPUT
          elif [[ "${{ matrix.suffix }}" == "-s 6 -p 6" ]]; then
            echo "url_s6p6=${url}" >> $GITHUB_OUTPUT
          fi

          sleep 300

  transcribe:
    name: 🧠 Transcribe and Push to Archive
    needs: record
    runs-on: ubuntu-latest
    strategy:
      matrix:
        pair:
          - { suffix: "s4p4", url: "${{ needs.record.outputs.url_s4p4 }}" }
          # - { suffix: "s5p5", url: "${{ needs.record.outputs.url_s5p5 }}" }
          # - { suffix: "s6p6", url: "${{ needs.record.outputs.url_s6p6 }}" }

    steps:
      - name: Clone Whisper-CPP (tanpa dotfiles)
        run: |
          git clone https://github.com/Sparkplugx1904/Whisper-CPP.git temp_whisper
          cd temp_whisper
          shopt -s extglob
          mv !(.?*) ../
          cd ..
          rm -rf temp_whisper

      - name: Setup FFmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3
      - name: Setup FFmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3
      - name: Setup FFmpeg
        uses: FedericoCarboni/setup-ffmpeg@v3

      - name: Install dependencies
        run: pip install requests pydub internetarchive

      - name: Download audio file
        run: |
          echo "[ INFO ] Mengunduh audio dari ${{ matrix.pair.url }}"
          curl -L -o "audio_${{ matrix.pair.suffix }}.mp3" "${{ matrix.pair.url }}"

      - name: Transcribe audio
        run: |
          echo "[ INFO ] Transcribing audio_${{ matrix.pair.suffix }}.mp3..."
          python3 -u transcriptor_cpp.py "audio_${{ matrix.pair.suffix }}.mp3" medium

      - name: Upload transcript artifact
        uses: actions/upload-artifact@v4
        with:
          name: vot-transcript-${{ matrix.pair.suffix }}
          path: transcripts/

      - name: Push transcript to Archive.org
        env:
          MY_ACCESS_KEY: ${{ secrets.MY_ACCESS_KEY }}
          MY_SECRET_KEY: ${{ secrets.MY_SECRET_KEY }}
          ARCHIVE_URL: ${{ matrix.pair.url }}
        run: |
          item_id=$(basename "$ARCHIVE_URL")
          echo "[ INFO ] Uploading transcript to $ARCHIVE_URL..."
          ia upload "$item_id" transcripts/ --retries 3 \
            --metadata="subject:Transcription" \
            --metadata="description:Auto transcription added by GitHub Actions" \
            --access-key "$MY_ACCESS_KEY" --secret-key "$MY_SECRET_KEY"
